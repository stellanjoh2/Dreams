Place downloadable 3D assets here. They are served at `/assets/…` (e.g. `school_of_fish.glb` → `/assets/school_of_fish.glb`).

**Butterflies:** `butterflies.glb` — scattered on platform tops, raised **`BLOCK_UNIT * 2.05`** above `topY` so they clear plants/floor; scaled ~**1.575** max extent. Avoids jump pads, moving elevator tiles, crystal anchors, and each other (`ButterflyScatterSystem.ts`). Prefer a clip named like *fly / flutter / wing / flap / idle*; otherwise the first clip is used. Drop the file at `public/assets/butterflies.glb`.

**Stylized clouds (sky ring):** `stylized_clouds_pack_vol_01.glb` — loaded by `StylizedCloudRing.ts`. Only the **largest** meshes (by world AABB) are used as templates, up to `MAX_CLOUD_SHAPES`; small filler meshes are ignored. Few instances (`CLOUD_COUNT`), `CLOUD_TARGET_SIZE` + `CLOUD_SCALE_MAX_MULT` variance, **`RING_INNER` / `RING_OUTER`**, and **`ORBIT_CLEARANCE_BEYOND_PLAYFIELD`** around the playfield center. **`HEIGHT_MIN_ABOVE_TOP` / `HEIGHT_MAX_ABOVE_TOP`** sit well above the map so clouds clear **bg mountains** while keeping the original horizontal ring. **Materials:** `tuneCloudMaterial` (`CLOUD_ALBEDO_DIM` / `CLOUD_EMISSIVE_FROM_ALBEDO`). `baseHeight` blends toward `WATER_SURFACE_Y`. Clouds **slerp** `lookAt` (`TURN_SMOOTH`).

**Cactus enemy:** `low_poly_cactus_enemy.glb` — placed on platform tiles without plants (`CactusEnemySystem`). Plays one **idle** clip (name heuristics, else first clip); faces the player. In dev, the console logs clip names and the chosen idle. List names locally: `npm run gltf:list-animations -- public/assets/low_poly_cactus_enemy.glb`. **Materials:** the file may embed many glTF maps; at runtime `sanitizeCactusShading` keeps **only base color** (`map`) — normal, ORM, emissive, env, displacement, physical extras, etc. are cleared.

**Cactus proximity SFX:** `cactus_enemy_indie_g_#3-1774200378182.wav` — loaded from `/assets/cactus_enemy_indie_g_%233-1774200378182.wav` (see `AUDIO_CACTUS_ENEMY_PROXIMITY_URLS`). The `#` in the filename is fine on disk; URLs must use `%23`.

**Mountain backdrop:** `low_poly_mountains.glb` — **`MOUNTAIN_RING_COUNT`** clones (~4) on a circle **outside** the farthest candy backdrop (`getBackdropFarFrameMetrics().farOuterR` + **`ORBIT_MARGIN_BEYOND_BACKDROP`**). The slot **nearest** `RESPAWN_ANCHORS[0]` gets **`SPAWN_NEAREST_EXTRA_MARGIN`** added so the south/south-west spawn view stays open. Base scale is **2×** the old single mountain (**`BASE_TARGET_MAX_EXTENT`**); each clone uses **`SIZE_MULTIPLIERS`** for variance. Pivots yaw toward the playfield center; tweak **`FACE_CENTER_YAW_BIAS`** / **`MODEL_UP_FIX`** if a mesh faces wrong.

**Fishing boat:** `fishing_boat_stylized.glb` — **two** instances on open water (`FishingBoatProp`): **`FISHING_BOAT_PLACEMENT_RIGHT`** (+X) and **`FISHING_BOAT_PLACEMENT_LEFT`** (−X, mirrored yaw). Procedural sway + optional GLB clips via `AnimationMixer`. Tweak `BOAT_ANCHOR_X_MAG` / `BOAT_Z` / `BOAT_YAW` / `MODEL_UP_FIX` in `FishingBoatProp.ts`.

**Reef fish pack:** `school_of_fish.glb` is expected to be a single scene with multiple species and a **swimming** animation clip (as in common Sketchfab/Unity-style packs). If your purchase shipped **separate** `.glb` files per species, drop them here and we can wire them in `FishSchoolsSystem` (see `MODEL_URL` / future multi-URL list).

**Textures:** The original pack used **`KHR_materials_pbrSpecularGlossiness`** (diffuse + spec/gloss PNGs embedded in the GLB). **Three.js r183 no longer implements that extension**, so those maps were ignored and fish looked untextured. The active `school_of_fish.glb` here was converted with `gltf-transform metalrough` to **`pbrMetallicRoughness`** (baseColor + metallicRoughness, etc.). The unconverted file is kept as `school_of_fish_specgloss_source.glb` if you need it elsewhere.
